{
  "name": "@sqlink/browser",
  "version": "0.1.0",
  "private": true,
  "description": "Scenario 3: browser-side SQLite + sqlink extensions, hosted by @tegmentum/wasi-polyfill.",
  "type": "module",
  "scripts": {
    "transpile": "node scripts/transpile-extensions.mjs",
    "fixtures": "node scripts/build-fixtures.mjs",
    "link-composed-wasm": "node scripts/link-composed-wasm.mjs",
    "pretest": "npm run fixtures && npm run link-composed-wasm",
    "build": "vite build",
    "dev": "vite",
    "test": "playwright test",
    "test:install": "playwright install chromium"
  },
  "dependencies": {
    "@tegmentum/wasi-polyfill": "file:../../wasi-polyfill"
  },
  "devDependencies": {
    "@bytecodealliance/jco": "^1.10.0",
    "@playwright/test": "^1.49.0",
    "smol-toml": "^1.7.0",
    "vite": "^5.4.0"
  }
}
