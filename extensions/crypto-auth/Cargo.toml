[package]
name = "crypto-auth-extension"
version = "0.1.0"
edition = "2021"
description = """
Authentication / verification primitives — Argon2 password
hashing. Historically this umbrella also exposed JWT, TOTP, and
bcrypt; those have been split out into focused extensions
(extensions/jwt, extensions/totp, extensions/pwhash).

Functions:
  argon2_hash(password)                TEXT     PHC-format hash
  argon2_verify(hash, password)        INTEGER  0/1
  crypto_auth_version()                TEXT

Tier-3 cluster per PLAN-extensions-followups Phase E6.
"""
license = "MIT"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[features]
embed = ["dep:libsqlite3-sys", "dep:sqlite-embed"]

[dependencies]
wit-bindgen = { version = "0.44", features = ["macros"] }
wit-bindgen-rt = { version = "0.44.0", features = ["bitflags"] }
libsqlite3-sys = { version = "0.38", optional = true, default-features = false }
sqlite-embed = { path = "../../sqlite-embed", optional = true }
# Password hashing.
argon2 = "0.5"
password-hash = { version = "0.5", features = ["getrandom"] }
