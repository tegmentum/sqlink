/**
 * Tests for text extension
 * Generated by build-extension.py
 */

import { fileURLToPath } from 'url';
import { dirname, join } from 'path';

const __filename = fileURLToPath(import.meta.url);
const __dirname = dirname(__filename);

// Import extension callbacks
import * as callbacks from './extension-callbacks.js';

// Import SQLite extensible component
const sqlite = await import('../../../build/js-ext/sqlite-extensible.js');
const { lowLevel, extension } = sqlite;

let passed = 0;
let failed = 0;

function assert(condition, message) {
    if (condition) {
        passed++;
        console.log(`  ✓ ${message}`);
    } else {
        failed++;
        console.log(`  ✗ ${message}`);
    }
}

function assertEqual(actual, expected, message) {
    if (actual === expected) {
        passed++;
        console.log(`  ✓ ${message}`);
    } else {
        failed++;
        console.log(`  ✗ ${message}`);
        console.log(`    Expected: ${expected}`);
        console.log(`    Actual: ${actual}`);
    }
}

async function runTests() {
    console.log('text Extension Tests\n');

    // Open database
    const db = lowLevel.open(':memory:', { readwrite: true, create: true, memory: true });
    assert(db !== 0n, 'Database opened');


    // Test sqlean_version
    console.log('\n  Testing sqlean_version...');
    try {
        extension.registerScalarFunction(
            db, 'sqlean_version', 0,
            { deterministic: false },
            callbacks.FUNC_SQLEAN_VERSION
        );
        assert(true, 'sqlean_version() registered');

        // TODO: Add specific test cases for sqlean_version
        // const stmt = lowLevel.prepare(db, "SELECT sqlean_version(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'sqlean_version() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ sqlean_version() failed: ${e.message}`);
    }

    // Test text_substring
    console.log('\n  Testing text_substring...');
    try {
        extension.registerScalarFunction(
            db, 'text_substring', 2,
            { deterministic: false },
            callbacks.FUNC_TEXT_SUBSTRING
        );
        assert(true, 'text_substring() registered');

        // TODO: Add specific test cases for text_substring
        // const stmt = lowLevel.prepare(db, "SELECT text_substring(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'text_substring() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ text_substring() failed: ${e.message}`);
    }

    // Test text_slice
    console.log('\n  Testing text_slice...');
    try {
        extension.registerScalarFunction(
            db, 'text_slice', 2,
            { deterministic: false },
            callbacks.FUNC_TEXT_SLICE
        );
        assert(true, 'text_slice() registered');

        // TODO: Add specific test cases for text_slice
        // const stmt = lowLevel.prepare(db, "SELECT text_slice(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'text_slice() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ text_slice() failed: ${e.message}`);
    }

    // Test text_left
    console.log('\n  Testing text_left...');
    try {
        extension.registerScalarFunction(
            db, 'text_left', 2,
            { deterministic: false },
            callbacks.FUNC_TEXT_LEFT
        );
        assert(true, 'text_left() registered');

        // TODO: Add specific test cases for text_left
        // const stmt = lowLevel.prepare(db, "SELECT text_left(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'text_left() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ text_left() failed: ${e.message}`);
    }

    // Test left
    console.log('\n  Testing left...');
    try {
        extension.registerScalarFunction(
            db, 'left', 2,
            { deterministic: false },
            callbacks.FUNC_LEFT
        );
        assert(true, 'left() registered');

        // TODO: Add specific test cases for left
        // const stmt = lowLevel.prepare(db, "SELECT left(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'left() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ left() failed: ${e.message}`);
    }

    // Test text_right
    console.log('\n  Testing text_right...');
    try {
        extension.registerScalarFunction(
            db, 'text_right', 2,
            { deterministic: false },
            callbacks.FUNC_TEXT_RIGHT
        );
        assert(true, 'text_right() registered');

        // TODO: Add specific test cases for text_right
        // const stmt = lowLevel.prepare(db, "SELECT text_right(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'text_right() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ text_right() failed: ${e.message}`);
    }

    // Test right
    console.log('\n  Testing right...');
    try {
        extension.registerScalarFunction(
            db, 'right', 2,
            { deterministic: false },
            callbacks.FUNC_RIGHT
        );
        assert(true, 'right() registered');

        // TODO: Add specific test cases for right
        // const stmt = lowLevel.prepare(db, "SELECT right(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'right() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ right() failed: ${e.message}`);
    }

    // Test text_index
    console.log('\n  Testing text_index...');
    try {
        extension.registerScalarFunction(
            db, 'text_index', 2,
            { deterministic: false },
            callbacks.FUNC_TEXT_INDEX
        );
        assert(true, 'text_index() registered');

        // TODO: Add specific test cases for text_index
        // const stmt = lowLevel.prepare(db, "SELECT text_index(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'text_index() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ text_index() failed: ${e.message}`);
    }

    // Test strpos
    console.log('\n  Testing strpos...');
    try {
        extension.registerScalarFunction(
            db, 'strpos', 2,
            { deterministic: false },
            callbacks.FUNC_STRPOS
        );
        assert(true, 'strpos() registered');

        // TODO: Add specific test cases for strpos
        // const stmt = lowLevel.prepare(db, "SELECT strpos(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'strpos() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ strpos() failed: ${e.message}`);
    }

    // Test text_last_index
    console.log('\n  Testing text_last_index...');
    try {
        extension.registerScalarFunction(
            db, 'text_last_index', 2,
            { deterministic: false },
            callbacks.FUNC_TEXT_LAST_INDEX
        );
        assert(true, 'text_last_index() registered');

        // TODO: Add specific test cases for text_last_index
        // const stmt = lowLevel.prepare(db, "SELECT text_last_index(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'text_last_index() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ text_last_index() failed: ${e.message}`);
    }

    // Test text_contains
    console.log('\n  Testing text_contains...');
    try {
        extension.registerScalarFunction(
            db, 'text_contains', 2,
            { deterministic: false },
            callbacks.FUNC_TEXT_CONTAINS
        );
        assert(true, 'text_contains() registered');

        // TODO: Add specific test cases for text_contains
        // const stmt = lowLevel.prepare(db, "SELECT text_contains(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'text_contains() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ text_contains() failed: ${e.message}`);
    }

    // Test text_has_prefix
    console.log('\n  Testing text_has_prefix...');
    try {
        extension.registerScalarFunction(
            db, 'text_has_prefix', 2,
            { deterministic: false },
            callbacks.FUNC_TEXT_HAS_PREFIX
        );
        assert(true, 'text_has_prefix() registered');

        // TODO: Add specific test cases for text_has_prefix
        // const stmt = lowLevel.prepare(db, "SELECT text_has_prefix(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'text_has_prefix() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ text_has_prefix() failed: ${e.message}`);
    }

    // Test starts_with
    console.log('\n  Testing starts_with...');
    try {
        extension.registerScalarFunction(
            db, 'starts_with', 2,
            { deterministic: false },
            callbacks.FUNC_STARTS_WITH
        );
        assert(true, 'starts_with() registered');

        // TODO: Add specific test cases for starts_with
        // const stmt = lowLevel.prepare(db, "SELECT starts_with(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'starts_with() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ starts_with() failed: ${e.message}`);
    }

    // Test text_has_suffix
    console.log('\n  Testing text_has_suffix...');
    try {
        extension.registerScalarFunction(
            db, 'text_has_suffix', 2,
            { deterministic: false },
            callbacks.FUNC_TEXT_HAS_SUFFIX
        );
        assert(true, 'text_has_suffix() registered');

        // TODO: Add specific test cases for text_has_suffix
        // const stmt = lowLevel.prepare(db, "SELECT text_has_suffix(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'text_has_suffix() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ text_has_suffix() failed: ${e.message}`);
    }

    // Test text_count
    console.log('\n  Testing text_count...');
    try {
        extension.registerScalarFunction(
            db, 'text_count', 2,
            { deterministic: false },
            callbacks.FUNC_TEXT_COUNT
        );
        assert(true, 'text_count() registered');

        // TODO: Add specific test cases for text_count
        // const stmt = lowLevel.prepare(db, "SELECT text_count(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'text_count() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ text_count() failed: ${e.message}`);
    }

    // Test text_like
    console.log('\n  Testing text_like...');
    try {
        extension.registerScalarFunction(
            db, 'text_like', 2,
            { deterministic: false },
            callbacks.FUNC_TEXT_LIKE
        );
        assert(true, 'text_like() registered');

        // TODO: Add specific test cases for text_like
        // const stmt = lowLevel.prepare(db, "SELECT text_like(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'text_like() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ text_like() failed: ${e.message}`);
    }

    // Test text_split
    console.log('\n  Testing text_split...');
    try {
        extension.registerScalarFunction(
            db, 'text_split', 3,
            { deterministic: false },
            callbacks.FUNC_TEXT_SPLIT
        );
        assert(true, 'text_split() registered');

        // TODO: Add specific test cases for text_split
        // const stmt = lowLevel.prepare(db, "SELECT text_split(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'text_split() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ text_split() failed: ${e.message}`);
    }

    // Test split_part
    console.log('\n  Testing split_part...');
    try {
        extension.registerScalarFunction(
            db, 'split_part', 3,
            { deterministic: false },
            callbacks.FUNC_SPLIT_PART
        );
        assert(true, 'split_part() registered');

        // TODO: Add specific test cases for split_part
        // const stmt = lowLevel.prepare(db, "SELECT split_part(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'split_part() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ split_part() failed: ${e.message}`);
    }

    // Test text_join
    console.log('\n  Testing text_join...');
    try {
        extension.registerScalarFunction(
            db, 'text_join', -1,
            { deterministic: false },
            callbacks.FUNC_TEXT_JOIN
        );
        assert(true, 'text_join() registered');

        // TODO: Add specific test cases for text_join
        // const stmt = lowLevel.prepare(db, "SELECT text_join(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'text_join() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ text_join() failed: ${e.message}`);
    }

    // Test concat_ws
    console.log('\n  Testing concat_ws...');
    try {
        extension.registerScalarFunction(
            db, 'concat_ws', -1,
            { deterministic: false },
            callbacks.FUNC_CONCAT_WS
        );
        assert(true, 'concat_ws() registered');

        // TODO: Add specific test cases for concat_ws
        // const stmt = lowLevel.prepare(db, "SELECT concat_ws(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'concat_ws() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ concat_ws() failed: ${e.message}`);
    }

    // Test text_concat
    console.log('\n  Testing text_concat...');
    try {
        extension.registerScalarFunction(
            db, 'text_concat', -1,
            { deterministic: false },
            callbacks.FUNC_TEXT_CONCAT
        );
        assert(true, 'text_concat() registered');

        // TODO: Add specific test cases for text_concat
        // const stmt = lowLevel.prepare(db, "SELECT text_concat(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'text_concat() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ text_concat() failed: ${e.message}`);
    }

    // Test concat
    console.log('\n  Testing concat...');
    try {
        extension.registerScalarFunction(
            db, 'concat', -1,
            { deterministic: false },
            callbacks.FUNC_CONCAT
        );
        assert(true, 'concat() registered');

        // TODO: Add specific test cases for concat
        // const stmt = lowLevel.prepare(db, "SELECT concat(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'concat() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ concat() failed: ${e.message}`);
    }

    // Test text_repeat
    console.log('\n  Testing text_repeat...');
    try {
        extension.registerScalarFunction(
            db, 'text_repeat', 2,
            { deterministic: false },
            callbacks.FUNC_TEXT_REPEAT
        );
        assert(true, 'text_repeat() registered');

        // TODO: Add specific test cases for text_repeat
        // const stmt = lowLevel.prepare(db, "SELECT text_repeat(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'text_repeat() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ text_repeat() failed: ${e.message}`);
    }

    // Test repeat
    console.log('\n  Testing repeat...');
    try {
        extension.registerScalarFunction(
            db, 'repeat', 2,
            { deterministic: false },
            callbacks.FUNC_REPEAT
        );
        assert(true, 'repeat() registered');

        // TODO: Add specific test cases for repeat
        // const stmt = lowLevel.prepare(db, "SELECT repeat(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'repeat() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ repeat() failed: ${e.message}`);
    }

    // Test text_ltrim
    console.log('\n  Testing text_ltrim...');
    try {
        extension.registerScalarFunction(
            db, 'text_ltrim', -1,
            { deterministic: false },
            callbacks.FUNC_TEXT_LTRIM
        );
        assert(true, 'text_ltrim() registered');

        // TODO: Add specific test cases for text_ltrim
        // const stmt = lowLevel.prepare(db, "SELECT text_ltrim(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'text_ltrim() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ text_ltrim() failed: ${e.message}`);
    }

    // Test ltrim
    console.log('\n  Testing ltrim...');
    try {
        extension.registerScalarFunction(
            db, 'ltrim', -1,
            { deterministic: false },
            callbacks.FUNC_LTRIM
        );
        assert(true, 'ltrim() registered');

        // TODO: Add specific test cases for ltrim
        // const stmt = lowLevel.prepare(db, "SELECT ltrim(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'ltrim() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ ltrim() failed: ${e.message}`);
    }

    // Test text_rtrim
    console.log('\n  Testing text_rtrim...');
    try {
        extension.registerScalarFunction(
            db, 'text_rtrim', -1,
            { deterministic: false },
            callbacks.FUNC_TEXT_RTRIM
        );
        assert(true, 'text_rtrim() registered');

        // TODO: Add specific test cases for text_rtrim
        // const stmt = lowLevel.prepare(db, "SELECT text_rtrim(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'text_rtrim() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ text_rtrim() failed: ${e.message}`);
    }

    // Test rtrim
    console.log('\n  Testing rtrim...');
    try {
        extension.registerScalarFunction(
            db, 'rtrim', -1,
            { deterministic: false },
            callbacks.FUNC_RTRIM
        );
        assert(true, 'rtrim() registered');

        // TODO: Add specific test cases for rtrim
        // const stmt = lowLevel.prepare(db, "SELECT rtrim(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'rtrim() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ rtrim() failed: ${e.message}`);
    }

    // Test text_trim
    console.log('\n  Testing text_trim...');
    try {
        extension.registerScalarFunction(
            db, 'text_trim', -1,
            { deterministic: false },
            callbacks.FUNC_TEXT_TRIM
        );
        assert(true, 'text_trim() registered');

        // TODO: Add specific test cases for text_trim
        // const stmt = lowLevel.prepare(db, "SELECT text_trim(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'text_trim() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ text_trim() failed: ${e.message}`);
    }

    // Test btrim
    console.log('\n  Testing btrim...');
    try {
        extension.registerScalarFunction(
            db, 'btrim', -1,
            { deterministic: false },
            callbacks.FUNC_BTRIM
        );
        assert(true, 'btrim() registered');

        // TODO: Add specific test cases for btrim
        // const stmt = lowLevel.prepare(db, "SELECT btrim(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'btrim() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ btrim() failed: ${e.message}`);
    }

    // Test text_lpad
    console.log('\n  Testing text_lpad...');
    try {
        extension.registerScalarFunction(
            db, 'text_lpad', -1,
            { deterministic: false },
            callbacks.FUNC_TEXT_LPAD
        );
        assert(true, 'text_lpad() registered');

        // TODO: Add specific test cases for text_lpad
        // const stmt = lowLevel.prepare(db, "SELECT text_lpad(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'text_lpad() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ text_lpad() failed: ${e.message}`);
    }

    // Test lpad
    console.log('\n  Testing lpad...');
    try {
        extension.registerScalarFunction(
            db, 'lpad', -1,
            { deterministic: false },
            callbacks.FUNC_LPAD
        );
        assert(true, 'lpad() registered');

        // TODO: Add specific test cases for lpad
        // const stmt = lowLevel.prepare(db, "SELECT lpad(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'lpad() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ lpad() failed: ${e.message}`);
    }

    // Test text_rpad
    console.log('\n  Testing text_rpad...');
    try {
        extension.registerScalarFunction(
            db, 'text_rpad', -1,
            { deterministic: false },
            callbacks.FUNC_TEXT_RPAD
        );
        assert(true, 'text_rpad() registered');

        // TODO: Add specific test cases for text_rpad
        // const stmt = lowLevel.prepare(db, "SELECT text_rpad(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'text_rpad() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ text_rpad() failed: ${e.message}`);
    }

    // Test rpad
    console.log('\n  Testing rpad...');
    try {
        extension.registerScalarFunction(
            db, 'rpad', -1,
            { deterministic: false },
            callbacks.FUNC_RPAD
        );
        assert(true, 'rpad() registered');

        // TODO: Add specific test cases for rpad
        // const stmt = lowLevel.prepare(db, "SELECT rpad(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'rpad() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ rpad() failed: ${e.message}`);
    }

    // Test text_upper
    console.log('\n  Testing text_upper...');
    try {
        extension.registerScalarFunction(
            db, 'text_upper', 1,
            { deterministic: false },
            callbacks.FUNC_TEXT_UPPER
        );
        assert(true, 'text_upper() registered');

        // TODO: Add specific test cases for text_upper
        // const stmt = lowLevel.prepare(db, "SELECT text_upper(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'text_upper() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ text_upper() failed: ${e.message}`);
    }

    // Test text_lower
    console.log('\n  Testing text_lower...');
    try {
        extension.registerScalarFunction(
            db, 'text_lower', 1,
            { deterministic: false },
            callbacks.FUNC_TEXT_LOWER
        );
        assert(true, 'text_lower() registered');

        // TODO: Add specific test cases for text_lower
        // const stmt = lowLevel.prepare(db, "SELECT text_lower(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'text_lower() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ text_lower() failed: ${e.message}`);
    }

    // Test text_title
    console.log('\n  Testing text_title...');
    try {
        extension.registerScalarFunction(
            db, 'text_title', 1,
            { deterministic: false },
            callbacks.FUNC_TEXT_TITLE
        );
        assert(true, 'text_title() registered');

        // TODO: Add specific test cases for text_title
        // const stmt = lowLevel.prepare(db, "SELECT text_title(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'text_title() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ text_title() failed: ${e.message}`);
    }

    // Test text_casefold
    console.log('\n  Testing text_casefold...');
    try {
        extension.registerScalarFunction(
            db, 'text_casefold', 1,
            { deterministic: false },
            callbacks.FUNC_TEXT_CASEFOLD
        );
        assert(true, 'text_casefold() registered');

        // TODO: Add specific test cases for text_casefold
        // const stmt = lowLevel.prepare(db, "SELECT text_casefold(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'text_casefold() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ text_casefold() failed: ${e.message}`);
    }

    // Test text_replace
    console.log('\n  Testing text_replace...');
    try {
        extension.registerScalarFunction(
            db, 'text_replace', 3,
            { deterministic: false },
            callbacks.FUNC_TEXT_REPLACE
        );
        assert(true, 'text_replace() registered');

        // TODO: Add specific test cases for text_replace
        // const stmt = lowLevel.prepare(db, "SELECT text_replace(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'text_replace() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ text_replace() failed: ${e.message}`);
    }

    // Test text_translate
    console.log('\n  Testing text_translate...');
    try {
        extension.registerScalarFunction(
            db, 'text_translate', 3,
            { deterministic: false },
            callbacks.FUNC_TEXT_TRANSLATE
        );
        assert(true, 'text_translate() registered');

        // TODO: Add specific test cases for text_translate
        // const stmt = lowLevel.prepare(db, "SELECT text_translate(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'text_translate() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ text_translate() failed: ${e.message}`);
    }

    // Test translate
    console.log('\n  Testing translate...');
    try {
        extension.registerScalarFunction(
            db, 'translate', 3,
            { deterministic: false },
            callbacks.FUNC_TRANSLATE
        );
        assert(true, 'translate() registered');

        // TODO: Add specific test cases for translate
        // const stmt = lowLevel.prepare(db, "SELECT translate(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'translate() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ translate() failed: ${e.message}`);
    }

    // Test text_reverse
    console.log('\n  Testing text_reverse...');
    try {
        extension.registerScalarFunction(
            db, 'text_reverse', 1,
            { deterministic: false },
            callbacks.FUNC_TEXT_REVERSE
        );
        assert(true, 'text_reverse() registered');

        // TODO: Add specific test cases for text_reverse
        // const stmt = lowLevel.prepare(db, "SELECT text_reverse(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'text_reverse() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ text_reverse() failed: ${e.message}`);
    }

    // Test reverse
    console.log('\n  Testing reverse...');
    try {
        extension.registerScalarFunction(
            db, 'reverse', 1,
            { deterministic: false },
            callbacks.FUNC_REVERSE
        );
        assert(true, 'reverse() registered');

        // TODO: Add specific test cases for reverse
        // const stmt = lowLevel.prepare(db, "SELECT reverse(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'reverse() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ reverse() failed: ${e.message}`);
    }

    // Test text_length
    console.log('\n  Testing text_length...');
    try {
        extension.registerScalarFunction(
            db, 'text_length', 1,
            { deterministic: false },
            callbacks.FUNC_TEXT_LENGTH
        );
        assert(true, 'text_length() registered');

        // TODO: Add specific test cases for text_length
        // const stmt = lowLevel.prepare(db, "SELECT text_length(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'text_length() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ text_length() failed: ${e.message}`);
    }

    // Test char_length
    console.log('\n  Testing char_length...');
    try {
        extension.registerScalarFunction(
            db, 'char_length', 1,
            { deterministic: false },
            callbacks.FUNC_CHAR_LENGTH
        );
        assert(true, 'char_length() registered');

        // TODO: Add specific test cases for char_length
        // const stmt = lowLevel.prepare(db, "SELECT char_length(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'char_length() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ char_length() failed: ${e.message}`);
    }

    // Test character_length
    console.log('\n  Testing character_length...');
    try {
        extension.registerScalarFunction(
            db, 'character_length', 1,
            { deterministic: false },
            callbacks.FUNC_CHARACTER_LENGTH
        );
        assert(true, 'character_length() registered');

        // TODO: Add specific test cases for character_length
        // const stmt = lowLevel.prepare(db, "SELECT character_length(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'character_length() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ character_length() failed: ${e.message}`);
    }

    // Test text_size
    console.log('\n  Testing text_size...');
    try {
        extension.registerScalarFunction(
            db, 'text_size', 1,
            { deterministic: false },
            callbacks.FUNC_TEXT_SIZE
        );
        assert(true, 'text_size() registered');

        // TODO: Add specific test cases for text_size
        // const stmt = lowLevel.prepare(db, "SELECT text_size(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'text_size() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ text_size() failed: ${e.message}`);
    }

    // Test octet_length
    console.log('\n  Testing octet_length...');
    try {
        extension.registerScalarFunction(
            db, 'octet_length', 1,
            { deterministic: false },
            callbacks.FUNC_OCTET_LENGTH
        );
        assert(true, 'octet_length() registered');

        // TODO: Add specific test cases for octet_length
        // const stmt = lowLevel.prepare(db, "SELECT octet_length(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'octet_length() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ octet_length() failed: ${e.message}`);
    }

    // Test text_bitsize
    console.log('\n  Testing text_bitsize...');
    try {
        extension.registerScalarFunction(
            db, 'text_bitsize', 1,
            { deterministic: false },
            callbacks.FUNC_TEXT_BITSIZE
        );
        assert(true, 'text_bitsize() registered');

        // TODO: Add specific test cases for text_bitsize
        // const stmt = lowLevel.prepare(db, "SELECT text_bitsize(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'text_bitsize() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ text_bitsize() failed: ${e.message}`);
    }

    // Test bit_length
    console.log('\n  Testing bit_length...');
    try {
        extension.registerScalarFunction(
            db, 'bit_length', 1,
            { deterministic: false },
            callbacks.FUNC_BIT_LENGTH
        );
        assert(true, 'bit_length() registered');

        // TODO: Add specific test cases for bit_length
        // const stmt = lowLevel.prepare(db, "SELECT bit_length(...)");
        // lowLevel.step(stmt);
        // const result = lowLevel.column...(stmt, 0);
        // assertEqual(result, expected, 'bit_length() returns correct value');
        // lowLevel.finalize(stmt);

    } catch (e) {
        failed++;
        console.log(`  ✗ bit_length() failed: ${e.message}`);
    }

    // Close database
    lowLevel.close(db);

    // Summary
    console.log('\n' + '='.repeat(50));
    console.log(`Tests: ${passed + failed} total, ${passed} passed, ${failed} failed`);
    console.log('='.repeat(50) + '\n');

    if (failed > 0) {
        process.exit(1);
    }
}

runTests().catch(err => {
    console.error('Test error:', err);
    process.exit(1);
});
